# 开发日志 — 2026-05-31

> 项目：历史粘贴板  
> 阶段：阶段 1 — 项目初始化

---

## 今日完成事项

1. ✅ 创建项目文件夹结构（`docs/`、`devlog/`）
2. ✅ 编写需求文档 `docs/requirements.md`（功能清单、用户故事、非功能需求）
3. ✅ 编写技术规范 `docs/tech-spec.md`（技术栈、架构、DB设计、模块划分）
4. ✅ 编写设计规范 `docs/design-spec.md`（配色、字体、布局、交互、动画）
5. ✅ 编写开发执行步骤 `docs/development-plan.md`（7阶段计划）
6. ✅ 编写项目指引 `CLAUDE.md`（AI助手工作约定+文档索引）
7. ✅ 编写项目说明 `README.md`

## 遇到的问题

无（项目初始化阶段，未涉及技术实现）

## 待办事项

- [ ] 等待用户确认阶段 1 产出
- [ ] 确认后可进入阶段 2：数据库 + 剪贴板监听核心
- [ ] 阶段 2 前需确认用户电脑已安装 Python 3.11+

---

## 阶段 2 完成事项（追加）

1. ✅ 创建 `src/`、`src/ui/`、`src/utils/`、`resources/` 目录
2. ✅ 编写 `requirements.txt`（PySide6 + Pillow）
3. ✅ 安装依赖：PySide6 6.11.1, Pillow OK
4. ✅ 编写 `src/database.py`（9 项测试全部通过）
   - 建表 + 索引、文字/图片插入、查询/搜索、置顶切换、删除、过期清理
5. ✅ 编写 `src/clipboard_monitor.py`（监听器初始化 + 信号连接通过）
6. ✅ 编写 `src/main.py`（入口：初始化 DB → 启动监听 → 打印日志）
7. ✅ 端到端测试：程序设置剪贴板 → 监听器捕获 → DB 存储成功
8. ✅ 修复跨导入兼容（同时支持包导入和直接运行）

## 遇到的问题

1. **终端编码问题**：中文输出在 GBK 终端下显示乱码，不影响程序逻辑
2. **相对导入失败**：直接运行 src/ 下文件时 `from . import` 报错，已改为 try/except 兼容两种导入方式

## 待办事项

- [ ] 等待用户确认阶段 2 产出
- [ ] 阶段 3：系统托盘 + 主窗口 UI 框架
- [ ] 阶段 3 需要创建应用图标 `resources/icon.ico`

## 下一步计划

~~进入阶段 4~~ ✅ 已完成。下一步进入阶段 5。

---

## 阶段 3 完成事项（追加）

1. ✅ 用 Pillow 生成应用图标 `resources/icon.ico`（粉色圆角剪贴板图案，256×256）
2. ✅ 编写 `resources/style.qss` — 完整淡粉色主题（60+ 条样式规则）
3. ✅ 编写 `src/ui/main_window.py` — 主窗口框架（搜索栏 + 卡片区 + 状态栏）
4. ✅ 编写 `src/ui/toast.py` — 轻提示组件（滑入/淡出动画 + 冷却机制）
5. ✅ 更新 `src/main.py` — 系统托盘图标 + 右键菜单 + 窗口懒加载
6. ✅ 修复 Toast 冷却 + 静默复制 + 防抖机制

### 测试结果（11 项全部通过，已确认）

### 遇到的问题
- PowerShell 不支持 `&&`，需用 `;`
- 函数定义顺序错误（UnboundLocalError），已修复
- 点击卡片复制触发重复记录+Toast，已修复（静默复制+冷却）
- 截图导致 signal storm，已修复（防抖定时器 300ms）

---

## 阶段 4 完成事项（追加）

1. ✅ 创建 `src/ui/card_widget.py` — 正式卡片组件
   - 文字卡片：最多 3 行截断 + 省略号，悬停显示全文
   - 图片卡片：120px 缩略图，保持比例缩放
   - 置顶标记（📌）+ 相对时间显示
   - 左键点击 → 静默复制（文字/图片均支持）
   - 右键菜单：📋 复制 / 📌 置顶(取消置顶) / 🗑️ 删除
   - 悬停样式变化
2. ✅ 更新 `src/ui/main_window.py` — 集成 CardWidget
   - 替换占位卡片为正式 CardWidget
   - 新增删除确认对话框（粉色样式）
   - 置顶切换后自动刷新列表
   - 删除后自动刷新列表
3. ✅ 图片缩略图显示（QPixmap 加载 + 缩放）
4. ✅ 图片复制到剪贴板（QPixmap → setPixmap）

### 测试结果（全部通过）

| 测试项 | 结果 |
|--------|------|
| 卡片列表加载 | ✅ (6 条) |
| 置顶排序 | ✅ (第一条是置顶) |
| 搜索过滤 | ✅ ("天气" → 1 条) |
| 文字卡片创建 | ✅ |
| 删除功能 | ✅ (确认后删除 + 列表刷新) |
| 数据清理 | ✅ |

### 遇到的问题

- 无重大问题。

## 待办事项

- [ ] 等待用户确认阶段 4 产出
- [ ] 阶段 5：设置面板 + 保留天数 + 开机自启 + 过期清理

## 下一步计划

进入阶段 5：设置面板（保留天数 1/3/5 天、开机自启开关、复制提示开关）+ 配置持久化（JSON）+ 过期自动清理定时器。

1. ✅ 用 Pillow 生成应用图标 `resources/icon.ico`（粉色圆角剪贴板图案，256×256）
2. ✅ 编写 `resources/style.qss` — 完整淡粉色主题（60+ 条样式规则）
   - 全局字体、窗口背景、搜索栏、滚动条、按钮、卡片、菜单、Toast
3. ✅ 编写 `src/ui/main_window.py` — 主窗口框架
   - 搜索栏（🔍 图标 + 输入框 + 清除按钮）
   - 卡片滚动区域（QScrollArea + 粉色滚动条）
   - 底部状态栏（记录数 + ⚙ 设置按钮）
   - 空状态提示："还没有剪贴板记录"
   - 搜索防抖（300ms）+ 实时过滤
   - 简单的文字预览卡片（阶段 4 替换为完整卡片组件）
   - 点击卡片 → 复制到剪贴板
   - 关闭窗口 → 隐藏到托盘
4. ✅ 编写 `src/ui/toast.py` — 轻提示组件
   - 无边框 + 屏幕右下角定位
   - 滑入动画（300ms）+ 停留（1.5s）+ 淡出（200ms）
   - 粉色背景 + 白色文字："已复制到历史记录 ✓"
5. ✅ 更新 `src/main.py` — 完整应用入口
   - 系统托盘图标 + 右键菜单（打开/退出）
   - 单击托盘 → 切换窗口显示/隐藏
   - 主窗口懒加载（首次打开才创建）
   - 剪贴板捕获 → Toast 提示 + 列表自动刷新
   - 样式表自动加载
   - 兼容源码运行和 PyInstaller 打包

### 测试结果（11 项全部通过）

| 测试项 | 结果 |
|--------|------|
| 系统托盘可用 | ✅ |
| 图标文件加载 | ✅ |
| 主窗口创建 | ✅ |
| Toast 创建 | ✅ |
| 剪贴板捕获 → DB 存储 | ✅ |
| 列表刷新 | ✅ |
| 状态栏计数 | ✅ |
| 搜索过滤 | ✅ |
| 样式表加载 | ✅ |
| 窗口显示/隐藏 | ✅ |
| 程序正常退出 | ✅ |

### 遇到的问题

- 无重大问题。测试全部通过。

---

## 阶段 5 完成事项（追加）

1. ✅ 创建 `src/utils/config.py` — 配置管理（JSON 读写）
2. ✅ 创建 `src/ui/settings_dialog.py` — 设置面板（保留天数/开机自启/复制提示）
3. ✅ 创建 `src/utils/cleanup.py` — 过期清理调度器（每小时自动清理）
4. ✅ 设置按钮接入、通知开关生效、卡片宽度自适应修复
5. ✅ 更新样式表 — 添加开关（Toggle）样式

### 遇到的问题

1. **卡片右侧被窗口裁切**：QSS margin 在 QScrollArea 内宽度计算不准 → 改用布局 margin + resizeEvent 延迟同步 viewport 宽度
2. **首次打开卡片仍被裁切**：resizeEvent 触发时 Qt 内部布局未完成 → 加 showEvent + QTimer.singleShot(0)

### 测试结果（全部通过）

| 测试项 | 结果 |
|--------|------|
| 配置读写 | ✅ |
| 设置面板 UI | ✅ |
| 保留天数切换 | ✅ |
| 通知开关生效 | ✅ |
| 清理调度器运行 | ✅ |
| 卡片宽度自适应 | ✅ |

## 待办事项

- [x] 阶段 5 已完成
- [ ] 阶段 6：开机自启 + PyInstaller 打包 .exe

## 下一步计划

进入阶段 6：开机自启（注册表/启动文件夹）+ PyInstaller 打包为 .exe + 打包后路径兼容测试。
